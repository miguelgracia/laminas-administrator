# This is a sample build configuration for Other.
# Check our guides at https://confluence.atlassian.com/x/5Q4SMw for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: atlassian/default-image:2

pipelines:
  default:
    - step:
        script:
          - pipe: atlassian/ftp-deploy:0.3.3
            variables:
              USER: absconsultor.es
              PASSWORD: ydtnwA3PNg9K
              SERVER: absconsultor.es
              REMOTE_PATH: /
              LOCAL_PATH: /
              EXTRA_ARGS: "--exclude=data/config/development.config.php --exclude=data/config/autoload/development.local.php --exclude=data/config/autoload/zend-developer-tools.local-development"